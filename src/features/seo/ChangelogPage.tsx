/**
 * @deprecated Prefer `/releases` (`ReleasesPage`). Se mantiene solo por si
 * algún import antiguo aún apunta acá; la ruta `/changelog` redirige a
 * `/releases`.
 */
export { ReleasesPage as ChangelogPage } from "@/features/releases/ReleasesPage";
